let ingredients = ["water", "flour", "salt", "oil", "tomatoes"];

/**
 * Use the reduce() method to transform the array of ingredients into a string like this:
 * 
 * "Pizza ingredients: water, flour, salt, oil, tomatoes"
 * 
 * Pay attention to not having the comma at the end
 */ 